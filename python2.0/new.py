print("Hello, World!");
print("Ankit rajak");
print("this is ankit rajak , a software devloper . know more about us");

# keyword - theseare reserved words in python which have special meaning and cannot be used as variable names or identifiers.
# ex - if, else, elif, for, while, break, continue, def, return, class, import, from, as, pass, global, nonlocal, assert, yield, try, except, finally, raise, with, lambda.
# identifier - these are the names given to variables, functions, classes, etc. in python. they must follow certain rules and conventions.
# rules for identifiers:
# 1. an identifier can only contain letters (a-z, A-Z), digits (
# 0-9), and underscores (_).
# 2. an identifier must start with a letter or an underscore, but cannot start with
# variable names cannot start with a digit.
# 3. an identifier cannot be a keyword.
# two or more var
# if change any variable name then we have to change that variable name in whole program otherwise it will give error.
# variable names should be meaningful and descriptive to improve code readability.
# python - dynamically typed language, which means that you do not need to declare the type of a variable when you create it. the type of a variable is determined at runtime based on the value assigned to it.
# example of variable assignment
x = 10  # x is an integer variable
y = 20  # y is an integer variable
z = x + y  # z is an integer variable